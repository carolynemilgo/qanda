import Ember from 'ember';

export default Ember.Component.extend({
  // updateVotes: function(answer, number) {
  //     var votes = answer.get('votes');
  //     answer.set('votes', votes + number);
  //     answer.save();
  // actions:{
  // up: function () {
  //   return (this.get('answer'), 1);
  //   },
  //   down: function () {
  //   return (this.get('answer'), -1);
  //   },
});
